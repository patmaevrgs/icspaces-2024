import { Button } from "@mui/material";

const PrintSched_Admin = () => {
  return (
    <Button
      color="secondary"
      variant="contained"
      style={{ fontWeight: "bold" }}
    >
      Print Schedule
    </Button>
  );
};

export default PrintSched_Admin;
