import { Typography } from "@mui/material";

const Status_Admin = () => {
  return (
    <Typography variant="h3" mt={10}>
      Status Pop up for Admin - note: this is not a page, its a component.,
      routing will be removed once done.
    </Typography>
  );
};

export default Status_Admin;
